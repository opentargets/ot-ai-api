[project]
name = "ot-ai-api"
version = "0.1.0"
description = "API for Open Targets serving as a centralized hub for AI-driven endpoints"
readme = "README.md"
authors = [{ name = "Open Targets", email = "contact@opentargets.org" }]

requires-python = ">=3.12"
dependencies = [
    "defusedxml>=0.7.1",
    "fastapi[standard]",
    "httpx>=0.28.1",
    "openai>=2.6.1",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.32.0",
]

[dependency-groups]
lint = ["ruff>=0.7.1"]
